---
- name: start new test container
  docker_container:
    name: "{{ container_name }}"
    image: check-yum-content
    recreate: true
    command: sleep 300

- name: add container to inventory
  add_host:
    name: "{{ container_name }}"
    ansible_connection: docker
  changed_when: false
